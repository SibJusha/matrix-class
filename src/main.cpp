/* #include <iostream>
#include <algorithm>
#include <vector>

int main() {

    std::vector<int> vec;
    vec.size
    int N, k;
    std::cin >> N >> k;
    int * a = new int[N];
    for (int i = 0; i < N; i++) {
        a[i] = k;
    }
    matrix A(N), B(N), C(N), D(N), K(N, a);
    delete[] a;
    std::cin >> A >> B >> C >> D;
    std::cout << (A + B * ~C + K) * ~D;
    return 0;
} */